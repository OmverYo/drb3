##### 기본
python3-venv
# 파이썬 가상환경 생성 및 관리 라이브러리 

##### 가상환경
KorToBraille
# "한글" 문구를 점자(유니코드)로 변환해 주는 라이브러리
# 1. 왜 한글로 제한한 이유
# : 우리 문화재 설명을 비지니스 타겟으로 하였기 때문에, 한글에 집중.
# 2. 유일한 변환 라이브러리도 아닌데 이 lib을 선택한 이유
# : 1) 라이브러리 탐색 플랫폼 pypi 에서 "점자" 로 검색 되는 라이브러리(한글 지원)
# : 2) 가장 최신 릴리즈 된 라이브러리(다른 lib 들과의 호환 우려)
# https://pypi.org/project/KorToBraille/

psycopg2-binary
# postgresql DB 관리 및 제어 라이브러리

openCV
# 파이썬을 통한 이미지 처리 및 작업 라이브러리

numpy
# 각종 수학적 연산 작업 라이브러리

pyinstaller
# 파이썬 스크립트를 실행파일로 변환하는 라이브러리

pyyaml jinja2 typeguard
# pyinstaller 설치에 필요한 의존성 패키지